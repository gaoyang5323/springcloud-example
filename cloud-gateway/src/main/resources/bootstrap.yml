spring:
  application:
    name: gateway
  profiles:
    active: test
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
      #nacos里对应的Data Id: ${prefix}-${spring.profile.active}.${file-extension}
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml



